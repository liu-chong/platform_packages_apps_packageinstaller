apply plugin: 'com.android.application'

android {
  compileSdkVersion 25
  buildToolsVersion "25.0.2"

  defaultConfig {
    minSdkVersion 15
    targetSdkVersion 25
    versionCode 1
    versionName "0.1.0"
  }

  sourceSets {
    main {
      manifest.srcFile 'AndroidManifest.xml'
      java.srcDirs = [ 'src' ]
      res.srcDirs = [ 'res' ]
    }
  }

  signingConfigs {
    release {
      storeFile new File("${project.projectDir}/key.jks")
      keyAlias "testkey"
      storePassword "49yu2EqqfCqy"
      keyPassword "f4p1AobV1r7U"
    }
  }

  buildTypes {
    release {
      signingConfig signingConfigs.release
    }
  }

  dependencies {
    compile 'com.android.support:support-v4:25.3.1'
  }
}

repositories { jcenter() }

buildscript {
  repositories { jcenter() }
  dependencies { classpath 'com.android.tools.build:gradle:2.3.1' }
}
